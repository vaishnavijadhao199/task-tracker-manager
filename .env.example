# API Configuration
# Leave empty to use mock/localStorage backend
# Set to your backend API URL when ready (e.g., https://api.example.com)
VITE_API_URL=
const USE_MOCK = true; 

# This is a demo task management application
# The app uses localStorage for mock data storage when VITE_API_URL is not set
